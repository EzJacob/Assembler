#ifndef PROJ1_SECOND_PASS_H
#define PROJ1_SECOND_PASS_H


/* the second pass of the assembler. Returns 0 if no errors were found. Otherwise, returns 1. */
int secondPass(FILE* fptr, Node** labels,char* fname);


#endif
